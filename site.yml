---
# Entry point: provisions the full cluster.
# Run: ansible-playbook site.yml
# Run single layer: ansible-playbook playbooks/gpu.yml
- import_playbook: playbooks/common.yml
- import_playbook: playbooks/gpu.yml
